# Goal

I'd like the settings file to be stored in a more standard location, such as `~/.config/did-stuff/config.json`. This is a common location for configuration files on Unix-like systems, and it's where I'd expect to find a configuration file for a command-line tool like this.
